import React from 'react';
import { shallow } from 'enzyme';
import Leaderboad from './index';

// to do
describe('Leaderboad Tests', () => {

  it('ensures that the board is sorted', () => {
  });

  it('ensures that the board the correct players on the board', () => {
  });

  it('ensures that the delete button works', () => {
  });

});
